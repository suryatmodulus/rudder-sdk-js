const NAME = "Google Optimize";
const CNameMapping = {
  [NAME]: NAME,
  GoogleOptimize: NAME,
  Googleoptimize: NAME,
  GOOGLEOPTIMIZE: NAME,
  GOOGLE_OPTIMIZE: NAME,
};

export { NAME, CNameMapping };
